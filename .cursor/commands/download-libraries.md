Download Excalidraw icon libraries into the local cache.

## Steps

1. Run the download script:
   ```bash
   bash scripts/download_libraries.sh
   ```
   This downloads all `.excalidrawlib` files into `library_cache/`. Already-cached files are skipped.
